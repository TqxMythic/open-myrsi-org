node_modules
dist
dist-server

# --- Secrets & local env ---------------------------------------------------
# Ignore all real env files, but keep the committed template.
.env*
!.env.example

# --- AI / build tooling (kept local; not part of the public repo) ----------
.claude/
CLAUDE.md

# --- Historical migrations -------------------------------------------------
# A fresh self-hosted install only needs schema.sql (+ optional reset_db.sql).
# The incremental migration trail is kept locally, not shipped in the repo.
/migrations/

# --- Logs & incident/debug artifacts (may contain UUIDs, IPs, scraped data) -
*.log
server-logs.txt
message.txt
*.har

# --- Local data, dumps & reports -------------------------------------------
legacy-portal
supabase-reports
supabase/backups
supabase/.temp
supabase-dump.sql
gitbook-docs
file_list.txt
coverage

# --- Backups & transitional artifacts --------------------------------------
*.bak

# --- OS & editor cruft -----------------------------------------------------
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# --- Build cache -----------------------------------------------------------
*.tsbuildinfo
.vite/
vite.config.*.timestamp-*
